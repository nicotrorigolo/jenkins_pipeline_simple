@Library('tests') _

pipeline {
    agent any

    stages {
        stage('Hello') {
            steps {
                echo 'Hello World'
            }
        }
        stage('Hi bash') {
            steps {
                sh '''
                echo "Salut bg !"
                '''
            }
        }
    }
}
